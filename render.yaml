services:
  - type: web
    name: whatsapp-bulk-sender
    env: docker
    plan: free
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 3000
      - key: DATA_PATH
        value: /app/.baileys_auth
